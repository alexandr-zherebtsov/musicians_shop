import 'package:flutter/material.dart';

class AppStyles {
  static const double clipRadius = 4;
  static const double fieldRadius = 16;
  static const BoxConstraints constraints = BoxConstraints(maxWidth: 600);
}
